#!/bin/bash
File= /home/ankit/word=  "
noword=0

if [ -e "$File" ]
then
echo "File found"
else
echo "File not found"
fi

word_count=" $(grep -i -o $word $File | wc -l )"

if [ word_count -ne noword ]
then
echo "word found"
echo "$word_count"
else
echo "no such word found"
fi
